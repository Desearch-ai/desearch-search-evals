# Copy to .env and fill in your API keys.

DESEARCH_API_KEY=
OPENAI_API_KEY=
OPENROUTER_API_KEY=
TAVILY_API_KEY=
EXA_API_KEY=

# Write-scoped token for uploading runs to the HuggingFace dataset.
# Optional: without it, scripts/upload_to_hf.py is a no-op and runs stay local.
HF_TOKEN=
# Override the default dataset repo (desearch/desearch-search-evals) if needed.
# HF_DATASET_REPO=
